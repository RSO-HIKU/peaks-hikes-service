apiVersion: v1
kind: Service
metadata:
  name: {{ include "peaks-hikes-service.fullname" . }}-nodeport
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "peaks-hikes-service.labels" . | nindent 4 }}
spec:
  type: NodePort
  selector:
    {{- include "peaks-hikes-service.selectorLabels" . | nindent 4 }}
  ports:
    - port: {{ .Values.service.nodePort.port }}
      targetPort: {{ .Values.service.nodePort.port }}
      nodePort: {{ .Values.service.nodePort.nodePort }}
