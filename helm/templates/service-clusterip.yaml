apiVersion: v1
kind: Service
metadata:
  name: {{ include "peaks-hikes-service.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "peaks-hikes-service.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  selector:
    {{- include "peaks-hikes-service.selectorLabels" . | nindent 4 }}
  ports:
    - port: {{ .Values.service.clusterIP.port }}
      targetPort: {{ .Values.service.clusterIP.port }}
